name: numeric-scan

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */3 * * *"
    - cron: "30 1-23/3 * * *"

permissions:
  contents: write

concurrency:
  group: yuming-numeric-scan
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 75

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run numeric scanner
        env:
          SCAN_CONCURRENCY: ${{ vars.SCAN_CONCURRENCY }}
          SCAN_BATCH_SIZE: ${{ vars.SCAN_BATCH_SIZE }}
          SCAN_BATCH_DELAY: ${{ vars.SCAN_BATCH_DELAY }}
          RDAP_TIMEOUT: ${{ vars.RDAP_TIMEOUT }}
          RDAP_RETRIES: ${{ vars.RDAP_RETRIES }}
        run: |
          python -u icp_query.py \
            --generate \
            --alphabet 0123456789 \
            --min-length 4 \
            --max-length 8 \
            --limit 200000 \
            --target unregistered-with-icp \
            --concurrency "${SCAN_CONCURRENCY:-32}" \
            --batch-size "${SCAN_BATCH_SIZE:-64}" \
            --delay "${SCAN_BATCH_DELAY:-0.1}" \
            --timeout 20 \
            --retries 1 \
            --rdap-timeout "${RDAP_TIMEOUT:-5}" \
            --rdap-retries "${RDAP_RETRIES:-0}" \
            --max-runtime-seconds 3600 \
            --state-file numeric_scan_state.json \
            --matches-file numeric_scan_matches.json \
            -o latest_numeric_run_matches.json

      - name: Commit scan state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          for file in numeric_scan_state.json numeric_scan_matches.json latest_numeric_run_matches.json; do
            if [ -e "$file" ]; then
              git add "$file"
            fi
          done
          if git diff --cached --quiet; then
            echo "No scan changes to commit."
          else
            git commit -m "Update numeric scan state"
            git pull --rebase origin ${{ github.ref_name }}
            git push origin HEAD:${{ github.ref_name }}
          fi
